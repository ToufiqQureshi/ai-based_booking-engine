
# Claude Code worktrees (temporary agent workspaces)
.claude/worktrees/

# Security
.env
.env.*
# Negations must have no space after '!' and match the full path to work.
!.env.example
!frontend/.env.example
secrets.json

# Dependencies
node_modules
/backend/venv
/backend/.venv
__pycache__/
*.py[cod]
*$py.class

# Build
dist
dist-ssr
build
*.local

# System & Editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.log
cloudflared_config.yml

# Databases & Cache
*.db
*.sqlite
*.sqlite3
.pytest_cache/

# Stray submodule gitlink that repeatedly breaks the Cloudflare Pages build
# (gitlink with no .gitmodules URL). Ignore so it can't be re-added by accident.
decodo-api-docs/
